'use client';

import React from "react";
import {SignOutButton} from "@clerk/nextjs";

const Page = () => {
    return  (
        <SignOutButton>Log Out</SignOutButton>
    );
};

export default Page;
